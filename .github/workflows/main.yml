name: main
on: 
  push:
    branches:
    - main
  schedule:
    - cron: "54 23 * * *"
jobs:
  build:  
    runs-on: ubuntu-latest
    steps:
      - name: Running
        uses: actions/checkout@v3
      
    env:
      KEY_1: ${{ secrets.KEY_1 }}
      KEY_2: ${{ secrets.KEY_2 }}
      KEY_3: ${{ secrets.KEY_3 }}
      KEY_4: ${{ secrets.KEY_4 }}
      run: |
        echo "Running!"
